{
  "name": "trazo",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de gestión de inventarios con trazabilidad de consumo por cliente y proyecto. Backend NestJS (arquitectura hexagonal) + Frontend Next.js. Ver specs/001-gestion-inventarios/ para la especificación completa.",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/compartido",
    "backend",
    "frontend"
  ],
  "scripts": {
    "postinstall": "npm run build -w @trazo/compartido",
    "db:up": "docker compose up -d db",
    "dev:backend": "npm run start:dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "compartido:build": "npm run build -w @trazo/compartido",
    "verificar": "npm run lint -w backend && npm run lint -w frontend && npm run typecheck -w backend && npm run typecheck -w frontend && npm run test -w backend",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
