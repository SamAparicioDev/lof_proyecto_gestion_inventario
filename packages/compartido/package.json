{
  "name": "@trazo/compartido",
  "version": "0.1.0",
  "private": true,
  "description": "Contrato compartido entre backend y frontend: esquemas de validación Zod (una sola fuente de verdad — Principio IV) y tipos de la API REST. Se compila a dist/ en el postinstall de la raíz.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
