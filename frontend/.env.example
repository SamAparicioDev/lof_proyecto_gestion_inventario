# Variables de entorno del frontend Trazo.
# Copia este archivo a frontend/.env

# URL del backend NestJS al que Next.js reenvía las llamadas /api/* (ver next.config.ts).
# En desarrollo es el backend local; en producción, la URL interna del servicio backend.
BACKEND_URL="http://localhost:4000"
